DB_PASSWORD=
SUPABASE_PUBLIC_KEY=sb_publishable_

# Supabase – serwer (middleware, API)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=

# Supabase – przeglądarka (Realtime, auth w JS). Musi to być URL projektu Supabase (jak wyżej), NIE adres aplikacji (np. localhost:3000).
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
PUBLIC_SUPABASE_KEY=

OPENROUTER_API_KEY=

# Optional. Required only for POST /api/auth/delete-account (Admin API to remove user from Auth).
# Never expose this key to the client.
# SUPABASE_SERVICE_ROLE_KEY=